"""Pure Python core — no framework, no SQLAlchemy, no web dependencies.

Contains only standard-library utility functions (including zip and path
handling); nothing here is a web/framework/sqlalchemy dependency.
"""
